'use strict';

console.log('--- loading: global variables');

const instructions = `Click an operation, you'll be prompted to enter a number.
  you will be prompted until the number is valid.
  to cancel the operation, click "cancel".`;

let lastResult = 0;
